spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
    group:
      "local": "localPort, localRDB, localJPA, localAws, localSecret"
      "dev": "devPort, devRDB, devJPA, devAws, devSecret"
  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
